program example(input, output);
    var x, y : real ;
    z, w : array [ 10..20, 30..40 ] of integer ;
    r, q : array [ 0..9, 30..40 ] of boolean ;
    l, m, n : char ;
    function myfunction ( a, b : integer ; c, d : real) : integer ;
        begin

        end;
    procedure myprocedure ;
        begin 
           
        end;
    begin
        z[13, 34] := myfunction(z[15, 35], w[15, 35], x, y);
        myprocedure
    end.